# BUILD-HANDOFF — t_27ed7897 · Nick Salazar website

Written 2026-09-04T23:19:18-05:00 by the team lead (Claude Code 2.1.261, `--model claude-fable-5-1 --effort high`, interactive, PID 39738: `39738   48:21 Ss+`). Branch `feat/t_27ed7897`. Source folder `/Users/scottscheferman/Nick Salazar Website/` untouched (22 files, SHA256 before = after, independently recomputed by the lead: `media/originals.sha256`, `media/originals-after.sha256`).

## Preview (verified)
- **http://127.0.0.1:8124/** — lead's loopback-only server (`python3 -m http.server 8124 --bind 127.0.0.1` from `site/`, PID in `tests/server-8124.pid`). `curl -sI` → `HTTP/1.0 200 OK`; all 24 referenced local resources 200; zero external references (`tests/http-check.log`, `tests/http-check.json`).
- Other servers seen on this machine and **left untouched**: the orchestrator's 127.0.0.1:8123 (PID 71830, began returning empty responses at ~23:00 and was not restarted or killed), 127.0.0.1:8817 and 100.70.189.85:8817 (PIDs 81810/85784, not started by this team; the tailnet-bound one is outside this build's loopback-only scope).
- Exact viewing steps for Scott: `site/README.md`.

## Engine — DONE-MEANS 1 (evidence, not flags)
- Lead + three named in-process teammates in team `session-17191e92`: `media-preparation`, `narrative-content`, `visual-motion`. Runtime team config (copied), teammate `meta.json` (`"model":"claude-fable-5-1","taskKind":"in_process_teammate"`) and transcript field counts (every assistant turn `"model":"claude-fable-5-1"`, `"effort":"high"`): `engine/ENGINE-EVIDENCE.md`, `engine/captures/<ts>/`, continuous 5-second change snapshots of the live team/task dirs: `engine/snapshots/` (55+ snapshots), lead runtime + docs recheck: `engine/00-lead-runtime.md`.
- Direct teammate-to-teammate messages (SendMessage, sender→recipient, timestamps, first lines): 35+ logged in `engine/ENGINE-EVIDENCE.md` — markup contract (narrative↔visual, both directions), asset table + provenance Q&A (media↔narrative), measured palette + video specs (media→visual), integration/favicon/caption exchanges, plus lead steers. Teammate self-logs: `media/TEAMMATE-REPORT.md`, `copy/TEAMMATE-REPORT.md`, `hallmark/TEAMMATE-REPORT.md`.
- No Nous calls, no model substitution, no `-p`; docs rechecked in-session (agent-teams page fetched 2026-09-04).

## Site — DONE-MEANS 2
- `site/index.html` 28.8 KB · `styles.css` 33.6 KB (Hallmark stamp line 1, pre-emit line 2) · `tokens.css` 9.4 KB · `app.js` 13.1 KB (dependency-free, no network) · `fonts/` 5 self-hosted OFL woff2 · `assets/` 7 mp4 + 21 jpg (7 originals, 7 posters, 7 mobile posters) · total `site/` 35 MB (cap 70).
- Asset manifest reconciled from the filesystem: `site/assets/MANIFEST.md` (generated by `media/build-manifest.py`), provenance per file with pixel-correlation evidence: `media/reconciliation.md`, ffprobe/sips: `media/probe.json`. Finding: the source has **7** videos (the old spec's "8" was wrong); all 7 shipped; all 7 HEIC originals shipped as photos.
- Self-checks with raw logs: `tests/static-audit.md` (40/40 PASS after removing 4 checker false-positives), `tests/http-check.log`, `tests/browser-suite-final.log`, `tests/run-*.log`. Scripts: `tests/static_audit.py`, `tests/http_check.py`, `tests/browser_suite.mjs` (Playwright 1.62.1 from the hermes-agent node_modules, chromium 151 — no installs), `tests/run_all.sh`, `tests/serve.sh`.

## A–Q + screenshots + scroll capture — DONE-MEANS 3
- Mapping with controlling overrides: `hallmark/AQ-CRITERIA-MAP.md`. Q (external review) is pending by design.
- Real captures: 124 PNGs under `browser/w320|w375|w414|w768|w1440/` (hero, every plate at 12/50/85% progress, every section, page end) + `browser/first-render/` + `browser/fix-check/`; scroll recording `browser/scroll-capture-1440.mp4` (8.9 MB, 1280-wide re-encode of the Playwright webm).
- Implemented: name largest/heaviest type at all 5 widths (h1 72–240 px vs word pairs ≤128 px) in footage-orange; credentials in footage-yellow (sky-blue retired after pixel measurement: `media/palette-confirmation.md`); 7 video plates with unique word pairs (`copy/word-pairs.md`); progressive shade 0.15→0.72 with copy silver→ink-max; differential layers (media −12%, shade ±2%, pair +34→−14% + scale, story +48→−18%, caption pinned) on CSS view-timelines with a JS `--progress` fallback (`hallmark/motion-notes.md`); finite page with reachable contact + footer; N5 pill nav (native `<a>` + `<details>` sheet), Ft5 footer; pointer light on fine pointers only; honest non-sending assignment-brief utility (copy/download verified: `browser/downloaded-assignment-brief.txt`).

## Hallmark — DONE-MEANS 4
- Installed skill v1.1.0 (`~/.claude/skills/hallmark/.agents/skills/hallmark`, git d5f260677316b03dba81aad33a76a334ed78a771). Real-browser Zipline study (14 screenshots desktop/mobile, 13 raw probes incl. measured 0.3× hero parallax, corrections to the old text-only DNA): `hallmark/zipline-study/STUDY.md`. No Zipline assets/copy/brand reused.
- 58-gate matrix with evidence: `hallmark/SLOP-TEST.md` (47 PASS, 1 PASS* recorded override, 6 N/A, 4 browser-resolved in the addendum). Pre-emit critique (lead's independent scoring): `hallmark/PRE-EMIT.md`. Project memory: `site/.hallmark/log.json`.
- Source-grounded copy audit (every claim → source line, forbidden-claims rg checklist incl. the operator's liability/authority sweep): `copy/copy-audit.md`. Lead's two copy corrections applied (plate 02 ending; both license sentences defer scope to the credential).

## Accessibility / motion / performance / interaction — DONE-MEANS 5 (`browser/RESULTS.md`, 46 PASS · 1 FAIL)
PASS: no horizontal overflow at any width/progress; name largest; no 2-line affordances; keyboard reaches 20 stops with instant rings; skip link; nav pill links + Enter; brief assembles/copies/downloads with "Nothing was sent" status and zero external requests; reduced-motion (videos never play, 0 long animations, light off); autoplay denied (posters, no errors); slow network 400 kbps (DOMContentLoaded ≈7.2 s, h1 visible, 0 mp4 bytes before scroll); offscreen pause; lazy attach (last two videos unloaded until near view); scroll-timeline-unsupported JS fallback drives shade/media; console clean (favicon is a `data:` URI).
FAIL (explicit, not fabricated): **contrast worst-pixel** — 64 of 514 samples; every sample's mean clears threshold (min 4.74:1). Two genuine narrow-width defects behind it, both sent to visual-motion at close-out: (a) story paragraph overlaps the pinned caption while a plate is entering at ≤414 px (`w320/plate02-p12.png`); (b) hero name at 320 over bright sky 2.76:1 worst / 5.4:1 mean (3:1 floor). The remaining ≈1.0 readings are the fixed pill occluding rows directly under it after an anchor jump (sampler artifact + ordinary fixed-nav behaviour). See `hallmark/SLOP-TEST.md` addendum.

## README / hashes / no publish — DONE-MEANS 6
- `site/README.md`: exact local steps, verified URL, pre-publication dependencies (contact destination not supplied → honest brief utility; remove `noindex`; credentials unverified; AI plates captioned), evidence manifest pointer.
- `analysis/verification/HASHES.sha256`: SHA256 of every file under `site/` and `analysis/verification/` (excluding the orchestrator's subtree and the live snapshot stream). Verify: `shasum -a 256 -c analysis/verification/HASHES.sha256`.
- Nothing published, pushed, tunnelled or sent; no contact details invented; no package installs; no writes outside the workspace except Claude's own team/session files.

## Commands / results summary
`tests/run_all.sh` (server → http_check → static_audit → browser_suite → ffmpeg) plus the individual invocations logged in `tests/browser-suite-final.log`, `tests/http-check.log`, `tests/static-audit.md`; media: `media/build-manifest.py`, `media/extract-palette.py`; engine: `engine/capture-engine-evidence.py`, `engine/snapshot-team-dir.sh`; hashes: `make_hashes.sh`.

## Residual risk / unresolved
1. RESOLVED at 23:18 (styles.css/tokens.css by visual-motion): the plate caption now fades in only once the story has arrived (lead recheck at 320, plate 02 progress 0.12: story/caption line boxes do not intersect, caption opacity 0 — `browser/fix-check/m320-plate02-p12-recheck.png`), and the hero name over bright sky now measures 5.63:1 worst pixel / 6.43:1 mean at 320 and 5.69 / 6.35 at 375 (was 2.76 worst). The full-suite `browser/RESULTS.md` predates this fix and still shows the 64 worst-pixel readings; the targeted rechecks above are the post-fix evidence.
2. RESOLVED: stamp fields on `styles.css` line 1 now carry the measured contrast summary and mobile pass list.
3. No contact destination exists (pre-publication dependency); credentials are client-supplied and unverified; all video plates are AI re-stagings and are captioned as such — do not re-caption.
4. External independent review (criterion Q / redcell) has not run; this handoff is implementation-only and does not mark the website done.
5. Filename/scene mismatch kept on purpose (no renames allowed): `07-yard.mp4` is the indoor dolly scene, `photo-yard.jpg` the outdoor flat-rack yard; copy follows the visible content, not the filename.
6. The orchestrator's 8123 server is wedged; the 8817 servers are not this team's and were left running.
7. `saveData` path is code-reviewed only (not simulated in headless Chromium).
