# Hallmark slop test — 58 gates (installed references/slop-test.md, skill v1.1.0, git d5f260677316b03dba81aad33a76a334ed78a771)

Lead audit of site/index.html, site/styles.css, site/tokens.css, site/app.js. PASS = the gate question answers NO with the cited evidence; PASS* = passes with a recorded brief-driven override; PENDING-BROWSER = resolved by the final browser run (see the addendum at the bottom, filled after that run).

| gate | question (abridged from source) | result | evidence |
|---|---|---|---|
| 1 | Is the display font Inter, Roboto, Open Sans, Poppins, Lato, or a system default? | PASS | display = Big Shoulders Display, body = Geist (tokens.css --font-display/--font-body); no Inter/Roboto/system default as display |
| 2 | Is there a purple-to-blue (or cyan-to-magenta) gradient anywhere — including a `background-clip: text` gradient headline? *Genre note: atmospheric allows radial… | PASS | no purple/blue or cyan/magenta gradients; no background-clip:text anywhere (rg 'background-clip' = 0); gradients are two-stop shade/veil/silver only (tokens.css) |
| 3 | Is there a 3-equal-column card grid with icon-above-heading tiles? | PASS | no 3-equal-column icon-card grid; services = hairline rows 5/7 split; why = 5/4/3 uneven columns; no icons |
| 4 | Is any card nested inside another card? | PASS | no card-in-card; sections use hairline rows, figures, and the plate veil — no nested bordered boxes |
| 5 | Is any card using a thick coloured left/right side-stripe border? | PASS | no side-stripe borders (rg 'border-left/border-inline-start' = 0 in styles.css) |
| 6 | Hero shape — centred-everything. Is the hero `min-height: 100vh` with everything centred, OR are the eyebrow, title, lede, AND CTA all stacked on the same centr… | PASS | hero is left-biased (name/creds/role/CTA left, meta caption right); atmospheric genre note also allows canvas-framed hero; not everything centred |
| 7 | Is pure `#000` or pure `#fff` used as a base colour anywhere? *Genre note: modern-minimal allows pure `#fff` paper (the Stripe / ElevenLabs school).* | PASS | paper oklch(12% 0.008 40), ink oklch(95% 0.006 80); pure #000/#fff not used (tokens.css :root) |
| 8 | Does the page reuse a structure it shouldn't — either the generic AI template (Hero → 3 features → CTA → footer), or the *same* structural fingerprint / macrost… | PASS | first Hallmark run in this project (.hallmark/log.json created this build); structure = Split Studio + video plates, not hero→3 features→CTA |
| 9 | Are sections separated only by equal whitespace, with no rule, no ornament, no colour shift — every section identical in rhythm? | PASS | rhythm varies: 160dvh sticky plates alternate with text sections; hairline rules + liquid-silver head rule + photo grid + veil changes |
| 10 | Is `transition-all` (or `transition: all`) used anywhere? (Specify the properties.) | PASS | static audit 'no transition: all' = none (tests/static-audit.md) |
| 11 | Is `hover:scale-105` (or any uniform hover-scale) applied across multiple unrelated elements? | PASS | no hover-scale (rg 'scale(1.0' on :hover = 0); hover = background/border/colour only |
| 12 | Are bouncy / overshoot easings (`cubic-bezier(0.34, 1.56, ...)`, etc.) used on UI state changes — buttons, modals, tooltips? (Reserve overshoots for physical in… | PASS | easings are the three named tokens only; static audit bounce check = none |
| 13 | Does any element have *more than one* hover effect at the same time (translate + scale + shadow + colour + rotate)? | PASS | hover changes background+border+colour (colour channel only), :active = translateY(1px) separately; no stacked translate+scale+shadow |
| 14 | Are you animating `width`, `height`, `top`, `left`, `margin`, or `padding` anywhere? | PASS | transitions/animations use transform, opacity, color, background/border-color only; static audit layout-prop check = none |
| 15 | Does the focus ring transition into existence (fade in)? (Focus rings must appear instantly — keyboard users need an immediate indicator.) | PASS | :focus-visible outline has no transition (outline not in any transition list); ring appears instantly |
| 16 | Is there a celebratory success toast for an action whose effect the user can already see? (Silent success is taste; toasts are for failures and invisible effect… | PASS | brief utility: silent inline status 'Copied. Nothing was sent.' (role=status), no celebratory toast |
| 17 | Are tooltip hover-delay and focus-delay equal? (Hover should delay 800–1000 ms; focus should be 0 ms.) | N/A | no tooltips on the page |
| 18 | Is auto-rotating content (carousel, banner, stats) lacking pause-on-hover-and-focus? (WCAG 2.2.2.) | N/A | no auto-rotating carousel/banner/stats; videos are ambient muted loops paused offscreen and under reduced-motion (app.js) |
| 19 | Is there a placeholder name "Jane Doe / John Smith" or a startup cliché (Acme, Nexus, Seamless, Unleash)? | PASS | no Jane Doe/John Smith/Acme etc. (copy audit §G; static audit banned-words = none) |
| 20 | Is the `/* Hallmark · macrostructure: <name> · ... */` stamp missing from the top of the CSS? (It must be present.) | PASS | stamp is line 1 of styles.css: '/* Hallmark · macrostructure: Split Studio (video-plate adaptation) …' |
| 21 | Did I default to the Specimen macrostructure (numbered left-margin labels + huge serif + asymmetric spans + typographic-only CTA) when the brief did not explici… | PASS | Specimen not used; genre atmospheric |
| 22 | Does any neutral / surface colour have `oklch(... 0 ...)` (zero chroma)? Pure greys read as flat. Tint every neutral toward the anchor hue — minimum 0.005 chrom… | PASS | every neutral carries ≥0.003 chroma (tokens: paper 0.008, ink 0.006, silver 0.006, muted 0.008, rule alpha overlays of a tinted white) |
| 23 | Does the accent colour cover more than ~5 % of any single viewport (count by area: solid fills, large headings in accent, full-bleed accent backgrounds)? If yes… | PASS* | accent footprint per viewport is small (one accented word per plate, CTA outline, focus ring) EXCEPT the hero name, which is accent-orange by explicit user brief (RECOVERY 'Nick's name … in a photo/video-derived bold primary accent'); recorded as a deliberate override in the stamp |
| 24 | Is any padding / gap / margin a value that isn't on the named spacing scale (`--space-3xs` … `--space-5xl`, multiples of 4 px)? Arbitrary `padding: 17px` is a t… | PASS | spacing uses --space-* 4-pt tokens throughout; ad-hoc px values are limited to 1px/2px rules and 0.35rem select glyphs |
| 25 | Is any prose container's `max-width` outside the 45–75 ch range? Measure must read; under 45 ch is choppy, over 75 ch loses the eye. | PASS | --measure 62ch, --measure-narrow 44ch; all prose containers use them |
| 26 | Does any interactive element lack `:focus-visible`, `:active`, OR `:disabled` styling? (Eight states is the rule. Default + hover is two; you need at least defa… | PASS | buttons: default/hover/focus-visible/active/disabled/loading/error/success in styles.css §5; inputs §6; nav links have hover/focus/active/aria-current |
| 27 | Is there any `transform` / `animation` keyframe that is NOT covered by a `@media (prefers-reduced-motion: reduce)` fallback? Every motion gets a reduced-motion … | PASS | @media (prefers-reduced-motion: reduce) block collapses every animation/transform to none and ≤150ms opacity (styles.css §8); app.js also marks videos static |
| 28 | If the page has a demo video, does it autoplay with sound, lack a `poster`, lack `fetchpriority="high"`, or use `loading="lazy"` on the LCP element? (LCP-killer… | PASS | hero video: no autoplay attribute (JS play), muted, poster present, preload=metadata; below-fold plates preload=none with data-src lazy attach; no loading=lazy on the hero LCP |
| 29 | If the page has an abstract background, is it more than one accent colour, more than ~5 % footprint, or animating mesh-gradient on the whole page? (Aurora blobs… | PASS | no abstract mesh/aurora background; the shade/vignette gradients are darkening layers over user media, not colour blooms |
| 30 | Icon tells. Does the page (a) mix two or more icon libraries (Material + Heroicons + Lucide on the same page), OR (b) use an emoji glyph (✨ 🚀 ⚡ 🔥 🎯 ✅) as a feat… | PASS | no icon library, no emoji; typography leads |
| 31 | If the page has illustration, did I default to a Lottie library when a hand-built SVG or pure-CSS shape would have worked? (Lottie is last resort, not the defau… | N/A | no illustration/Lottie |
| 32 | If I used the same archetype as a previous Hallmark output (per `.hallmark/log.json` or the latest macrostructure stamp), did I pick at least one different *var… | N/A | first run in project; no prior archetype to vary from |
| 33 | Does any visual-only `<svg>`, custom-art `<div>`, `<canvas>`, or decorative figure lack `aria-label` or `aria-hidden="true"`? Hand-built CSS art and SVG illustr… | PASS | decorative divs (.plate__shade, .hero__light) and videos carry aria-hidden=true; images have literal alt |
| 34 | Does the page horizontally scroll on any viewport between 320 px and 1920 px? Open the rendered page; drag the dev-tools width slider across that range. If a ho… | PENDING-BROWSER | html/body overflow-x: clip present (static audit); browser suite checks scrollWidth at 5 widths × every plate progress — see browser/RESULTS.md |
| 35 | For every decorative effect on text — highlighter `<mark>` / `<em>` band / accent stroke / underline — did I visually confirm the position and size? A highlight… | PASS | no highlighter/mark bands; underline only on footer links (1px, offset 2px) |
| 36 | Are interactive bars (nav, toolbar, command bar, hero CTA row, footer link strip) explicitly vertically centered? Default flex layouts inherit `align-items: str… | PASS | nav, buttons, brief actions and footer meta rows declare align-items: center/baseline; controls use line-height: 1 (--lh-ui) |
| 37 | Does the page use more than three distinct `font-family` families? Count: `--font-display`, `--font-body`, and at most one outlier (`--font-outlier` for wordmar… | PASS | two families (Big Shoulders Display + Geist); the '*Fallback' faces are size-adjusted local fallbacks of the same roles |
| 38 | Is the outlier face used in more than two slots on the page? The outlier is a register, not a third surface — wordmark + hero stat is the canonical pair, or wor… | N/A | no outlier face |
| 38a | Is any heading or display type italic (`font-style: italic` on `h1`–`h6`, a `.*__title`, `.hero__title`, a wordmark, a stat figure, a footer statement, or an `<… | PASS | no italic on any heading/display/wordmark/stat; the only font-style: italic is .prose em (body emphasis) |
| 39 | Do input / textarea / select fields handle every state correctly? Fail on any of these five: - Border-width shifts between states — default / hover / focus / er… | PASS | inputs keep 1px border in every state (state changes go to border-color/background/outline), outline-based focus ring (transparent at rest), input height = button height = --control-height 44px, helper slots reserved via min-height: 1lh, disabled = opacity+cursor+attribute |
| 40 | Contrast thresholds. Does any text, icon, or `:focus-visible` ring fail its threshold against its *computed* background? Pair every `color` declaration with its… | PENDING-BROWSER | text contrast measured against rendered pixels at 3 scroll positions per plate and in each section (browser suite contrast-samples.json) |
| 41 | The contrast failures that ship most often. Fail on any: - Button text ≈ button fill — if the computed text colour and fill are within 5 % lightness AND 0.05 ch… | PASS | --color-accent-ink defined and used on accent fills (::selection); dark sections all set light ink in the same rule (.plate, .section, .foot); button text vs fill: fill=ink/paper pairs, outline=transparent/ink |
| 42 | Nav fingerprint. Is the page's `<nav>` (or top-of-page `<header>` with role="banner") the AI default — wordmark-left + 4–5 inline text links centred-or-right + … | PASS | N5 floating pill (content-sized, fixed, blur, hairline), not the full-width wordmark+links+button bar |
| 43 | Footer fingerprint. Is the `<footer>` the AI default — 4 columns of links (Product / Company / Resources / Legal) + social-icon row + tiny copyright at the very… | PASS | Ft5 statement footer: one display line + wordmark + two muted notes; no 4-column link index |
| 44 | Hero fit — sits into the page and fits the fold. Two checks, both on the rendered hero: (a) Is `padding-block-end` ≥ 1.3× `padding-block-start`? Symmetric or to… | PENDING-BROWSER | hero padding-block-end ≥ 1.3× start (hero__copy 9rem/… vs 4rem) — verify fold at 1280×800 in the final run screenshots |
| 45 | Decorative-without-purpose. Does the hero contain a decorative element (cursor, scanline, gradient blob, abstract shape, ornament, badge, sticker) that has no s… | PASS | hero decoration = pointer light responding to interaction (HP3 cursor-spotlight, allowed) and the honest 'Illustrative footage' caption; no unmotivated ornaments |
| 46 | Invented metric. Does the page contain any quantitative claim — "10× faster", "saves 5 hours per week", "trusted by 50,000+ teams", "99.9 % uptime", "+47 % conv… | PASS | no invented metrics; the only numbers are 1600 (license), about ten years (supplied), © 2026 (copy audit §G) |
| 47 | Re-drawn chrome. Did Hallmark hand-build a fake browser bar (URL pill + traffic-light dots), a fake phone frame (rounded rectangle + notch + speaker slit), a fa… | PASS | no fake browser/phone/IDE chrome; figures are plain <figure><img> with hairline border |
| 48 | Mid-render token improvisation. Did Hallmark introduce any colour value (`#hex`, `oklch(...)`, `rgb(...)`, `hsl(...)`) or `font-family` declaration *outside* th… | PASS | static audit: every colour/font in styles.css references a token; raw values live only in tokens.css :root/@font-face |
| 49 | Two-line clickable text. Does any button label, primary nav link, footer link, tab label, breadcrumb, or CTA text wrap to two or more lines at any viewport betw… | PENDING-BROWSER | all affordances white-space: nowrap; browser suite counts rendered line boxes for every a/button/summary at 5 widths |
| 50 | Image-bearing grid track without `minmax(0, 1fr)`. Does any `grid-template-columns` (or `grid-template-rows`) containing a `1fr` track render an `<img>` / `<pic… | PASS | every image-bearing grid track is minmax(0, 1fr) (.work__grid, .creds__grid, .services__item, .facts__row) |
| 51 | Display headers without long-word wrap. Does any element rendering display-size text (`h1`, `.hero__display`, `.section__title`, `.skill-row__title`, hero-equiv… | PASS | overflow-wrap: anywhere; min-width: 0 on .hero__name, .plate__pair, .section__title, .services__name, .why__heading, .foot__line |
| 52 | Per-theme section-head override without mobile collapse. When a theme or variant overrides `.section__head { grid-template-columns: ... }` to anything other tha… | PASS | section heads are single-column stacks below 60rem (.section__inner collapses to one column; the 5/7 split applies only ≥60rem and the title/lede stack vertically within it) |
| 53 | CSS-only radio tab pattern that scroll-jumps. When implementing tab toggles via `<input type="radio">` siblings + `:checked` selectors, does the artifact either… | N/A | no radio-tab pattern |
| 54 | Section eyebrow / tag beside the heading (tag-left, header-right). Does any section render an eyebrow / number / mono-cap label (`01 · THE TOUR`, `02 / FEATURES… | PASS | no eyebrow/number beside a heading; plate captions sit BELOW the pair/story; section titles have no eyebrow |
| 55 | All-caps display heads with line-height < 1.0 → cap-collision on wrap. Does any display-size element (`.hero__display`, `.section__title`, `h1`, `h2`, or anythi… | PASS | uppercase display line-heights: name 1.0, pair 1.02, tight 1.1 — none below 1.0 (static audit) |
| 56 | Sticky element at `top: 0` below a sticky page-level nav → bleed. Does the artifact declare `position: sticky; top: 0;` on any element OTHER than the page's top… | PASS | the only top-0 sticky elements are .plate__frame (inside plates) and the credentials figure at top: var(--banner-height); the nav is position: fixed with --z-sticky-nav 300 above --z-sticky 200, so nothing paints over it |
| 57 | Studied DNA discarded for a catalog theme. Did a `study` diagnosis emit earlier in the conversation, AND does the build's CSS stamp's `theme:` field name a cata… | PASS | stamp theme field = 'studied-DNA (source: https://www.zipline.com)'; no catalog theme substituted |

Totals: {'PASS': 47, 'N/A': 6, 'PASS*': 1, 'PENDING-BROWSER': 4}

## Addendum — browser-resolved gates (34, 40, 44, 49)

Final browser run 2026-09-05T04:16Z against http://127.0.0.1:8124/ (analysis/verification/browser/RESULTS.md, 46 PASS / 1 FAIL):

- **Gate 34 (no horizontal scroll): PASS** — scrollWidth ≤ clientWidth at 320/375/414/768/1440 at page top, at three progress points of every plate, and at the page end (`w*: no horizontal overflow …` rows all PASS).
- **Gate 49 (no two-line clickable text): PASS** — rendered line-box count = 1 for every `a`/`button`/`summary` at all five widths (`w*: no clickable text wraps` rows all PASS; the pill CTA is hidden below 26rem so the pill fits at 320).
- **Gate 44 (hero fits the fold): PASS** — visual-motion's probe at 1280×800 (`hallmark/self-check/probe-1280-hero-fit.txt`: creds y=104, CTA bottom 687, caption bottom 756) and the lead's 1440 hero capture (`browser/w1440/00-hero.png`); padding-block-end ≥ 1.3× start.
- **Gate 40 (contrast, measured through the shader transition): PASS on mean, FAIL on worst pixel in 64 of 514 samples** — every sample's *mean* ratio clears its threshold (minimum 4.74:1). Worst-pixel readings of ≈1.0 come from other text inside the sampled box (the fixed nav pill occluding rows immediately after an anchor jump, and — a real defect — the story paragraph overlapping the pinned caption while a plate is entering at ≤414px, `browser/w320/plate02-p12.png`). The hero name at 320 reads 2.76:1 worst / 5.4:1 mean over bright sky (3:1 floor). Both defects were fixed by visual-motion at 23:18 and rechecked by the lead (caption opacity 0 at entry, no line-box intersection; hero name 5.63:1 worst at 320) — see BUILD-HANDOFF.md § Residual risk items 1–2. The stamp's `contrast:` field is therefore **"measured — mean pass; two narrow-width worst-pixel defects logged"**, not a clean pass.

## Addendum 2 — after the operator's 23:2x edits (shade lightened; contact email added)
- Gate 40: pairs, stories, hero copy still pass on mean at every sampled state; the 14px plate captions dropped to 2.5–3.9:1 and were fixed with a caption-only backplate token (`--color-caption-plate`) → 10–14:1 mean re-measured. Hero name at 1440 is marginal on mean (3.36:1, floor 3:1) because the thin shade leaves the yellow cargo unit under the orange name; left as the operator's aesthetic decision, recorded here.
- Gate 46/honest copy: the contact address is operator-supplied (`copy/OPERATOR-EDITS.md`), not invented; static audit rule updated to permit exactly that address.
- Gate 49: the operator's display-size email link wraps to two lines at 320/375/414 in the full-suite re-run; size-only fix requested (status in BUILD-HANDOFF.md).
