# Hallmark pre-emit self-critique — lead's independent scoring (1–5; <3 forces revision)

visual-motion's own stamp in styles.css line 2: `P5 H5 E4 S5 R4 V4`. Lead's scoring below is made from the rendered screenshots (browser/w*/), not from the CSS alone.

| axis | score | rationale (evidence) |
|---|---|---|
| Philosophy | 5 | The page argues one thing — an accountable individual who watches cargo and writes it down — and every section serves it: name first, licences explained, complete service list, literal photos, honest AI-plate captions, a brief-builder instead of a fake form (index.html; copy-audit.md). |
| Hierarchy | 5 | Name (240px at 1440, 72px at 320) > word pairs (≤128px) > section titles > body; credentials sit beside the name in a second accent; browser check "h1 is the largest type" passes at all five widths (browser/RESULTS.md). |
| Execution | 4 | Tokens only, hairlines 1px/0.12, 44px controls, instant focus rings, inputs keep 1px borders; first render exposed three defects (credentials accent hue, a mid-word break in one services heading at 1440, the 320px nav pill overflow) — fixed/being fixed by visual-motion and re-verified in the final run. Not a 5 until the final run is clean. |
| Specificity | 5 | Palette measured from the actual clips (media/palette-confirmation.md), word pairs tied to what is visible in each plate (copy/word-pairs.md), services verbatim from the client list, captions literal. Nothing here could be another surveyor's page. |
| Restraint | 4 | Accents appear on the name, one word per plate, focus/CTA outlines only; no icons, no cards, no gradients on text. One deliberate excess by brief: the hero name is a large accent field (recorded override, gate 23). |
| Variety | 4 | First Hallmark run in this project so no repetition risk; within the page, plates alternate copy side (left/right), sections vary between hairline rows, a 12-column photo grid and a 5/4/3 three-point block. The seven plates share one structural rhythm on purpose (the brief asks for a continuous storytelling scroll). |

All axes ≥ 3 → no revision pass triggered by this critique; defects found by measurement are tracked in browser/RESULTS.md and the handoff.
